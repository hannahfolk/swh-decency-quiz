{
  "name": "swh-decency-quiz",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "concurrently \"cd server && yarn watch\" \"cd client && yarn start\"",
    "install": "cd server && yarn install && cd ../client && yarn install",
    "build": "cd client && yarn build"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
